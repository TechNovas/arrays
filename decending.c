#include <stdio.h>

int main() {
    int size;
    printf("Input element store in first array: ");
    scanf("%d", &size);
    int des[size];

    for(int i=1; i<size; i++)
    printf("%d",des[i]);
    // int des[3];

}